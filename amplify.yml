version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build step for static prototype"
    build:
      commands:
        - echo "Preparing artifacts"
  artifacts:
    baseDirectory: ./
    files:
      - "**/*"
  cache:
    paths: []
